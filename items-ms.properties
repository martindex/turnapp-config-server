server.port=${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
configuracion.texto=Configurando entorno default
custom.text=mi texto
custom.other=mi texto otro
